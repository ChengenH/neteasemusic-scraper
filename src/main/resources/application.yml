server:
  port: 8098
  context-path: /
spring:
  datasource:
    url: jdbc:mysql://****:3306/db_name?useUnicode=true&characterEncoding=utf8
    username: root
    password: password
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
